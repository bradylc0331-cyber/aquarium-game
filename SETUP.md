# 聖經樂園活動現場架設指南

這份指南寫給活動當天負責架設的志工，不需要程式背景。

## 需要準備

- 一台筆電（建議使用最新版 Chrome）
- 一台電視、投影機或大螢幕，用 HDMI 接筆電
- 一台能向下拍桌面的 USB webcam 或文件攝影機（建議 1080p）
- 印表機、A4 紙、彩色筆或蠟筆
- 穩固的攝影機支架與均勻桌面照明

一般透過 USB 接上電腦、能在 Chrome／Zoom 裡選到的 Webcam 可直接使用。只能透過 App、IP 位址或 RTSP 觀看的 Wi-Fi/IP Camera 不會直接出現在瀏覽器攝影機清單，需要另接虛擬攝影機軟體。

## 活動前一天：列印 A4 QR 塗色紙

1. 開啟網站首頁，點「🖨️ 列印 A4 QR 人物塗色紙」，或直接開啟 `templates/print.html`。
2. 按「列印全部」。列印設定選 **A4、橫向、100%／實際大小**，不要選「符合頁面」。
3. 每位人物都是一張完整 A4；四角有黑色定位方塊，上方有專屬 QR Code。依參加人數各印 10～20 張。
4. 不需要裁紙，也不需要另外列印校正墊。提醒小朋友不要塗到 QR Code 和四個黑色方塊。
5. 先挑一張上色作為測試紙，活動開始前走一次完整掃描流程。

## 活動當天：開機和自動對位

1. 將電視或投影機接上筆電，使用延伸桌面會比較方便操作。
2. 把攝影機架在桌面正上方，鏡頭向下。放入 A4 測試紙，預覽畫面要拍到整張紙與四個黑色定位方塊，四周保留少量空白。
3. 啟動網站：
   - 點「🖥️ 開啟聖經樂園畫面」，把這個分頁移到大螢幕並按 F11 全螢幕；活動期間保持開啟。
   - 點「📷 開啟掃描控制台」，把這個分頁留在筆電螢幕。
4. 在控制台的「② 攝影機」選擇正確裝置，按「開啟攝影機」。瀏覽器詢問鏡頭權限時要按允許。
5. 系統看到四個定位方塊後會自動拉正 A4，接著讀取 QR Code 並切換到正確人物；通常不需要按校正或手動選人物。
6. 「④ 即時對位預覽」會顯示拉正後的 A4 畫面，上面疊一層淡淡的人物線稿。若自動對位失敗，才使用「手動重新對位」。
7. 勾選「啟用自動拍攝與延遲登場」。狀態顯示 QR 已辨識後即可開始。

活動中攝影機不要移動。紙張稍微放歪沒關係，四角定位方塊會讓系統逐張重新拉正。

## 活動進行方式

1. 將完成的整張 A4 塗色紙放到攝影機下，確認四角定位方塊與 QR Code 都沒有被手遮住。
2. 系統會自動判斷是挪亞、摩西、大衛等人物，並自動拉正紙張，不必由工作人員先選人物。
3. 手離開畫面並保持作品不動。系統偵測穩定約一秒後會自動拍攝，下方會顯示去背結果。
4. 狀態顯示「已自動拍攝」後，把塗色紙拿走。系統要確認上一張已移除，才會準備拍下一張。
5. 作品會排入等待隊列，隨機經過 30–60 秒後自動出現在大螢幕，不需要再按送出。
6. 若自動偵測受光線影響，可用人物按鈕選擇正確角色，再使用「📸 手動掃描（備援）」及「✅ 送到聖經樂園」。

## 瀏覽器煙霧測試

活動前請至少確認一次：

- `index.html` 三個入口都能開啟。
- `display.html` 背景等比例鋪滿；人物會自然踏步，草葉與河面有動態。
- `control.html` 放入不同人物紙時會依 QR 自動切換人物；七個人物按鈕仍可手動備援。
- 故意把紙放歪一點，確認四角定位後的預覽仍會自動拉正。
- 完成一次自動拍攝，確認同一張不會重複拍；拿走後可偵測下一張。
- 等待 30–60 秒後，投影頁能自動收到作品；再測一次手動備援流程。
- `templates/print.html` 共七頁完整 A4 橫式人物紙；每頁都有四角定位方塊與不同 QR，列印縮放為 100%。
- 開啟瀏覽器開發者工具時沒有紅色 JavaScript 錯誤，也沒有素材 404。

## 常見狀況

| 狀況 | 處理方式 |
|---|---|
| A4 一直無法自動對位 | 改善桌面照明、避免反光，確認整張 A4 和四角方塊完整入鏡，再調整偵測門檻 |
| QR 無法辨識人物 | 手離開上方 QR、把紙攤平並確認以 100% 列印；必要時用人物按鈕手動備援 |
| 掃描結果透明或缺一塊 | 確認使用新版 A4 QR 塗色紙，四角與人物都完整入鏡 |
| 自動模式一直沒有拍攝 | 讓手離開畫面、保持紙張不動；改善照明並確認印刷線稿清楚。必要時使用手動掃描備援 |
| 同一張作品沒有再次拍攝 | 這是防重複機制；先將作品完整拿走，等狀態回到「待機」再放下一張 |
| 攝影機清單是空的 | 在網址列旁的網站權限中允許攝影機，重新整理控制台後再試 |
| 樂園只顯示漸層 | 確認 `assets/backgrounds/bible-world.png` 存在且可讀；漸層是正常保底畫面 |
| 投影頁重新整理後人物消失 | 正常，人物只保存在本次頁面狀態；活動期間不要重新整理 `display.html` |
| 想用兩台電腦分開掃描與顯示 | 目前透過同一瀏覽器的 `BroadcastChannel` 傳送，必須在同一台筆電的兩個分頁中使用 |
