# 活動現場架設指南

寫給主日活動當天負責架設的志工看，不需要懂程式。

## 需要準備的東西

- 一台筆電（跑瀏覽器用，建議 Chrome）
- 一台電視或大螢幕，用 HDMI 接筆電
- 一台可以往下拍桌面的攝影機（USB webcam 架在桌上型支架、或文件掃描機平台）
- 印表機、A4 紙、彩色筆/蠟筆
- 剪刀（把塗色紙沿虛線裁下用）

## 活動前一天：印東西 + 架設

1. 打開筆電瀏覽器，開啟這個網站的網址，點「🖨️ 列印校正墊與塗色紙」，或直接開
   `templates/print.html`。
2. 按「列印全部」。瀏覽器的列印設定裡，**縮放要選「100%」或「實際大小」**，
   不要選「符合頁面大小」，不然校正墊跟塗色紙的實際尺寸會對不起來。
3. 第一頁是**校正墊**，只印一張。把它固定貼在攝影機平台上（例如用透明膠帶四角
   貼牢），確保之後不會被碰到移動位置。
4. 後面幾頁是**每種生物的塗色紙**，各印個 10～20 張備用（依小朋友人數估）。
   沿著虛線外框剪下來，剪的時候盡量留著那條邊，方便小朋友放紙時對齊。

## 活動當天：開機架設

1. 電視接上筆電，延伸桌面或當第二螢幕都可以。
2. 攝影機接上筆電，架在校正墊正上方，鏡頭正對桌面往下拍，整張校正墊（含四個
   黑色角標）都要在畫面裡，四周留一點空白邊界。
3. 開瀏覽器，打開網站首頁：
   - 點「🖥️ 開啟水族箱畫面」，這個分頁**拖到電視螢幕上、按 F11 全螢幕**，
     全程開著給大家看。
   - 再點「📷 開啟掃描控制台」，這個分頁留在筆電螢幕上操作。
4. 在掃描控制台頁面：
   - 「② 攝影機」選對攝影機、按「開啟攝影機」，畫面應該看得到校正墊。
   - 「③ 校正」按下「校正」。成功會顯示綠色「已校正完成」。
     - 如果失敗（紅色訊息），先檢查校正墊四個角是不是都在畫面裡、有沒有被
       陰影或反光蓋住，也可以拉一下「偵測門檻」滑桿再試一次。
   - 校正成功後，「④ 即時對位預覽」會看到攤平校正過的畫面，上面疊了一層淡淡
     的生物外框——這是提示小朋友的紙該放在哪裡。
5. 校正只需要做這一次。**中途攝影機或校正墊不要被移動**，移動了要重新校正。

## 活動進行中：怎麼帶小朋友玩

1. 志工/小朋友先在「① 選生物種類」選好剛剛拿到的塗色紙是哪一種生物。
2. 把小朋友塗好的紙放進即時預覽畫面裡淡淡外框對應的位置（實體桌面上，校正墊
   中間虛線框那裡）。
3. 按「📸 掃描」。下面會出現摳出來的生物顏色預覽。
   - 顏色怪怪的、紙沒放好 → 按「🔁 重新掃描」，調整紙的位置再試一次。
   - 滿意了 → 按「✅ 送到水族箱」，電視畫面上就會出現這隻剛塗好的生物游進水
     族箱，同時會有提示文字彈出來。
4. 下一位小朋友重複同樣的流程，種類可以在畫面上隨時切換。

## 常見狀況

| 狀況 | 處理 |
|---|---|
| 校正一直失敗 | 光線不足或太亮反光。加一盞燈打亮桌面，或調整「偵測門檻」滑桿 |
| 掃描出來整片透明、沒有顏色 | 紙沒放進虛線框內，或放得太偏，重新對齊再掃 |
| 攝影機清單是空的 | 瀏覽器詢問鏡頭權限時要按「允許」；如果不小心按了拒絕，要到瀏覽器網址列旁邊的鎖頭圖示重新開權限 |
| 水族箱畫面重新整理後生物都不見了 | 正常——生物只存在當下這次瀏覽的畫面裡，重新整理會重置。活動全程不要重新整理 `display.html` 那一頁 |
| 想同時開兩台電腦（一台掃描、一台顯示） | 目前版本靠同一個瀏覽器內的 `BroadcastChannel` 傳資料，兩台不同電腦之間傳不過去，需要都在同一台筆電的兩個分頁裡開 |
